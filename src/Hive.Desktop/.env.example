# Optional overrides for the local backend's admin credentials.
# These must match whatever the backend was started with via
# HIVE_ADMIN_USERNAME / HIVE_ADMIN_PASSWORD. If unset, both default to
# admin/admin123 (see README.md for details on changing the default).
# VITE_ADMIN_USERNAME=admin
# VITE_ADMIN_PASSWORD=admin123
